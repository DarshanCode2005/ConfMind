# Frontend Changes

## UI/UX
- Replaced custom markdown rendering with `react-markdown` and `remark-gfm` in `ChatWidget.tsx`.
- Fixed horizontal overflow in chat bubbles with `break-words`.
- Improved responsive design of the `ChatWidget` to prevent layout breakage on mobile/small screens.
- Updated grid spacing in `DashboardPage` for the Graph and Simulator components.
